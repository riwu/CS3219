language: node_js
node_js:
  - 8
install:
  - cd client && npm install
script:
  - npm run build
notifications:
  email: false
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master
